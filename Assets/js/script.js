// when a user clicks the start quiz button the start page disappears
// a new page polulates
//the new page has a question prompt with 4 choices
//the new page has a timer
// when then user clicks on the right choice the button turns green
//when the user clicks on the wrong answer the button turns red
//when you click you are taken to a new page with a new question
//if they chose the wrong answer time is taken away from the timer
// all of the above cycles again

// when button is clicked the page clears and question one is displayed


var question = document.querySelector ("#question");
var choices = Array.from(document.querySelectorAll(".choice-text"));
var submitAnswer= document.getElementById("#answer");
var finalScore = document.getElementById("#finalScore");
var userName = document.getElementById("#userName");

submitAnswer.addEventListener("click")

var getNewQuestion = function () [

]




var btn = document.querySelector(".btn button");

var element = document.getElementById("#btn");
element.addEventListener("click", function () {});

btn.onclick = () => {
  info_box.classList.add("activeInfo");
};

var startQuiz = function () {
  alert("This is an alert");
};

startQuiz();




